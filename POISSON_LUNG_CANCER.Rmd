---
title: "CÁNCER DE PULMÓN OHIO"
author: "María Paula Camargo Rincón"
date: "2026-03-02"
output:
---
```{r directorio y librerias, message=FALSE, warning=FALSE, include=FALSE}
#rm(list = ls())
library(dplyr) # para manejo de datos
library(sf) # para leer la base de datos
library(ggplot2) # para gráficos
library(tidyr) # para poner en formato largo la tabla
library(knitr) # para mostrar las tablas
library(ppcor) #correlación parcial
library(MASS) # binomial negativa
library(dglm) # modelos doblemente generalizados
library(lme4) # para hacer poisson con efecto aleatorio
library(glmmTMB) # para hacer binomial negativa con efecto aleatorio
library(pscl) # para modelo 0 inflado
library(psych) # para pairs.panels(), parte descriptiva 
library(performance) # verificar supuestos de modelos
library(glmtoolbox) # envelope
library(lmtest) #Para prueba breusch pagan y durbin watson
library(DHARMa) # para prueba de autocorrelación en residuales con glmer
library(tseries) # para pruebas de rachas
library(spdep) # para función poly2nb en vecindad criterio queen
library(tmap) #para graficar mapa
library(grid) # para mapa lisa
library(spatialreg) # para rezagar variables
```
# Introducción {#PrInv}

Este conjunto de datos se deriva de un estudio de mortalidad por cáncer de pulmón en Ohio realizado entre 1968 y 1978 a nivel de condado. La base de datos `ohiolung` contiene información para los 88 condados del estado de Ohio, (EE. UU.). La base de datos proviene del GeoDa (Center for Geospatial Analysis and Computation) y presenta 42 variables. En las estas se presentan datos referentes a la mortalidad por cáncer de pulmón estratificada por género (masculino/femenino) y raza (blanca/negra), junto variables geográficas básicas.

La siguiente tabla resume las variables principales de la base de datos:

```{r tabla1, echo=FALSE, message=FALSE, warning=FALSE}
library(sf)
library(dplyr)
library(kableExtra)
library(knitr)

variables <- data.frame(
  Variable = c("CountyID", "NAME", "FIPSNO", "AREA", "PERIMETER", "RECORD_ID", "COUNTYID",
               "LGRyy", "POPGRyy", "LGyy", "POPGyy"),
  Descripcion = c("ID secuencial del condado (orden alfabético)",
                  "Nombre del condado",
                  "Código FIPS del condado (numérico)",
                  "Área del polígono (condado)",
                  "Perímetro del polígono (condado)",
                  "ID único del registro",
                  "ID del condado",
                  "Casos de cáncer de pulmón para género G (M/F) y raza R (W/B) en año yy (1968, 1978, 1988)",
                  "Población en riesgo para género G y raza R en año yy",
                  "Total de casos de cáncer de pulmón por género G en año yy",
                  "Población total en riesgo por género G en año yy")
)

knitr::kable(variables, caption = "Definición de variables principales en la base de datos ohlung.shp")%>%
  kable_styling(
    font_size = 10,  # Tamaño de fuente reducido
    latex_options = c("striped", "scale_down"),  # Escalado automático
    full_width = FALSE
  ) %>%
  column_spec(2, width = "10cm") %>% # Ancho específico para descripción
 column_spec(1, bold = TRUE) # Ancho específico para variable



```
Se realizó una modificación del formato de la tabla para poder trabajar con las variables sexó (Femenino, Masculino), año(1968, 1978, 1988) y raza (Blanco, Negro)
```{r formato largo, echo=FALSE, message=FALSE, warning=FALSE}
ohlung <- st_read("ohiolung/ohlung.shp", quiet = TRUE) 

ohlung$casos_totales <- rowSums(
  ohlung[, c("LM68", "LF68", "LM78", "LF78", "LM88", "LF88")] %>% sf::st_drop_geometry())

ohlung$casos_totales68 <- rowSums(
  ohlung[, c("LM68", "LF68")] %>% sf::st_drop_geometry())

ohlung$casos_totales78 <- rowSums(
  ohlung[, c("LM78", "LF78")] %>% sf::st_drop_geometry())

ohlung$casos_totales88 <- rowSums(
  ohlung[, c("LM88", "LF88")] %>% sf::st_drop_geometry())

ohlung$pop_total <- rowSums(
  ohlung[, c("POPM68", "POPF68", "POPM78", "POPF78", "POPM88", "POPF88")] %>% sf::st_drop_geometry())

ohlung$pop_total68 <- rowSums(
  ohlung[, c("POPM68", "POPF68")] %>% sf::st_drop_geometry())

ohlung$pop_total78 <- rowSums(
  ohlung[, c("POPM78", "POPF78")] %>% sf::st_drop_geometry())

ohlung$pop_total88 <- rowSums(
  ohlung[, c("POPM88", "POPF88")] %>% sf::st_drop_geometry())

ohlung$tasa_68 <- ohlung$casos_totales68 / ohlung$pop_total68

ohlung$tasa_78 <- ohlung$casos_totales78 / ohlung$pop_total78

ohlung$tasa_88 <- ohlung$casos_totales88 / ohlung$pop_total88

ohlung$cambio_68_88 <- ohlung$tasa_88 - ohlung$tasa_68

ohlung$cambio_78_88 <- ohlung$tasa_88 - ohlung$tasa_78

ohlung$cambio_68_78 <- ohlung$tasa_78 - ohlung$tasa_68


ohlung_long <- ohlung %>%
  dplyr::select(COUNTYID, NAME, AREA, PERIMETER,
                LMW68, LFW68, LMB68, LFB68,
                POPMW68, POPFW68, POPMB68, POPFB68,
                LMW78, LFW78, LMB78, LFB78,
                POPMW78, POPFW78, POPMB78, POPFB78,
                LMW88, LFW88, LMB88, LFB88,
                POPMW88, POPFW88, POPMB88, POPFB88, geometry) %>%
  tidyr::pivot_longer(
    cols = dplyr::matches("^(LMW|LFW|LMB|LFB|POPMW|POPFW|POPMB|POPFB)\\d{2}$"),
    names_to = "temp",
    values_to = "value"
  ) %>%
  dplyr::mutate(
    gender = dplyr::if_else(stringr::str_detect(temp, "M"), "Male", "Female"),
    race = dplyr::if_else(stringr::str_detect(temp, "W"), "White", "Black"),
    year   = as.numeric(stringr::str_extract(temp, "\\d{2}")) + 1900,
    type   = dplyr::if_else(stringr::str_detect(temp, "^L"), "cases", "pop")
  ) %>%
  dplyr::select(-temp) %>%  # Funciona con dplyr::select() explícito
  tidyr::pivot_wider(names_from = type, values_from = value) %>%
  dplyr::mutate(
    gender = as.factor(gender),
    race = as.factor(race),
    year   = as.factor(year)
  ) %>% arrange(COUNTYID)

knitr::kable(head(ohlung_long, 10), caption = "Vista de los datos en formato largo")
```
```{r conteos iguales a 0, echo=FALSE}
ohlung_long %>%
  dplyr::select(cases) %>%
  dplyr::filter(cases == 0) %>%
  dplyr::summarise(count = n()) %>% 
  sf::st_drop_geometry()
```
Notese que tenemos de 1056 observaciones, 386 son 0 casos, esto puede representar un problema más adelante en los modelos, ya que puede tender a inflar la varianza.

```{r include=FALSE}
ohlung$tasa_white_68 <- rowSums(ohlung[,c('LMW68','LFW68')]%>% sf::st_drop_geometry()) / rowSums(ohlung[,c('POPMW68','POPFW68')]%>% sf::st_drop_geometry()) 
ohlung$tasa_black_68 <- rowSums(ohlung[,c('LMB68','LFB68')]%>% sf::st_drop_geometry()) / rowSums(ohlung[,c('POPMB68','POPFB68')]%>% sf::st_drop_geometry()) 

ohlung$tasa_white_78 <- rowSums(ohlung[,c('LMW78','LFW78')]%>% sf::st_drop_geometry()) / rowSums(ohlung[,c('POPMW78','POPFW78')]%>% sf::st_drop_geometry()) 
ohlung$tasa_black_78 <- rowSums(ohlung[,c('LMB78','LFB78')]%>% sf::st_drop_geometry()) / rowSums(ohlung[,c('POPMB78','POPFB78')]%>% sf::st_drop_geometry()) 

ohlung$tasa_white_88 <- rowSums(ohlung[,c('LMW88','LFW88')]%>% sf::st_drop_geometry()) / rowSums(ohlung[,c('POPMW88','POPFW88')]%>% sf::st_drop_geometry()) 
ohlung$tasa_black_88 <- rowSums(ohlung[,c('LMB88','LFB88')]%>% sf::st_drop_geometry()) / rowSums(ohlung[,c('POPMB88','POPFB88')]%>% sf::st_drop_geometry()) 
```



```{r descriptiva categoricas, echo=FALSE, fig.height=7, fig.width=10}
par(mfrow=c(1,2), bg = 'antiquewhite3')
boxplot(ohlung$tasa_white_68, col = "lightblue", main = "Año 68")
boxplot(ohlung$tasa_black_68, col = "pink" )
legend('topright',lty = 1:2,legend = c("White", 'Black'), col = c('lightblue','pink'))
boxplot(ohlung$tasa_white_78, col = "lightblue", main = "Año 68")
boxplot(ohlung$tasa_black_78, col = "pink")
legend('topright',lty = 1:2,legend = c("White", 'Black'), col = c('lightblue','pink'))
boxplot(ohlung$tasa_white_88, col = "lightblue", main = "Año 68")
boxplot(ohlung$tasa_black_88, col = "pink")
legend('topright',lty = 1:2,legend = c("White", 'Black'), col = c('lightblue','pink'))

tabla_conteos <- dplyr::bind_rows(ohlung_long %>% 
             dplyr::group_by(categoria=gender) %>% 
             dplyr::summarise(conteo=sum(cases/pop)),
           ohlung_long %>% 
             dplyr::group_by(categoria=race) %>% 
             dplyr::summarise(conteo=sum(cases/pop)),
           ohlung_long %>% 
             dplyr::group_by(categoria=year) %>% 
             dplyr::summarise(conteo=sum(cases/pop))) %>% 
  sf::st_drop_geometry()
knitr::kable(tabla_conteos,
             caption = "Tasas de incidencia por variable categórica")
```
En el primer gráfico

```{r}
plot(ohlung$pop_total,ohlung$casos_totales, col = 'antiquewhite3')
plot(ohlung$pop_total68,ohlung$casos_totales68, col = 'antiquewhite3')
plot(ohlung$pop_total78,ohlung$casos_totales78, col = 'antiquewhite3')
plot(ohlung$pop_total88,ohlung$casos_totales88, col = 'antiquewhite3')
```
En general los totales de casos y poblaciones en los distintos años se comportan de manera lineal.

```{r}
casos_condado <- ohlung_long %>% 
  group_by(NAME,year,gender) %>%
  summarise(tasas = sum(cases/pop, na.rm = TRUE), .groups = "drop")

top_condados <- ohlung_long %>%
  group_by(NAME) %>%
  summarise(total_tasas = sum(cases/pop, na.rm = TRUE)) %>%
  slice_max(total_tasas, n = 15)

ggplot(
  casos_condado %>% filter(NAME %in% top_condados$NAME),
  aes(
    x = NAME,
    y = tasas,
    fill = gender,
    group = interaction(year, gender)
  )
) +
  geom_col(
    position = position_dodge2(preserve = "single"),
    width = 0.8
  ) +
  coord_flip() +
  facet_grid(~ year) +
  labs(
    title = "Tasas de cancer por condado, year y género",
    x = "Condado",
    y = "Numero de casos",
    fill = "Genero"
  ) +
  theme_minimal()
```
Podemos ver más registros o casos en los hombres que en las mujeres en los distintos años y condados.

```{r}
top_condados2 <-  ohlung_long %>%  
  group_by(NAME) %>%
  summarise(total_tasas = sum(cases/pop, na.rm = TRUE),
            area = first(AREA)) %>% 
  slice_max(total_tasas, n = 15) %>% 
  sf::st_drop_geometry()
knitr::kable(top_condados2, caption = "Top 15 condados con tasas más altas")
```

## Exploración espacial

```{r}
tmap_arrange(
  tm_shape(ohlung) + tm_fill("tasa_68", palette="Reds"),
  tm_shape(ohlung) + tm_fill("tasa_78",palette="Reds"),
  tm_shape(ohlung) + tm_fill("tasa_88",palette="Reds")
)

```

Podemos ver que en los años se ha presentado un aumento de la tasa de incidencia de cancer de pulmón.



```{r}
tmap_arrange(
  tm_shape(ohlung) + tm_fill("cambio_68_88"),
  tm_shape(ohlung) + tm_fill("cambio_68_78"),
  tm_shape(ohlung) + tm_fill("cambio_78_88")
  
)
```
En este se realizó una diferencia de las tasas en los años, tal diferencia significa que si es positiva quiere decir que aumento, y si es negativa quiere decir que disminuyó. Podemos ver que comparando las tasas de los años de 1968 y 1988, casi no hay tasas negativas, lo que indica que en general entre 1968 y 1988 hubo un aumento de casos de cancer de pulmón. Por otro lado comparando los años 1968 y 1978, podemos ver más condados con tasas negativas, lo que indica que hubo una disminución, esto es importante porque si bien vimos en el anterior mapa que la tasa aumentaba conforme los años, no quiere decir que en todos los contados hubo un aumento. Por último entre los años 1978 y 1988 podemos ver que fueron más constantes las tasas, es decir que no hubo un aumento  signficativo en la mayoría de condados (resaltados por el verde claro), además tambien podemos ver que hay tasas que siguieron disminuyendo (morado).


```{r}
tmap_arrange(
  tm_shape(ohlung) + tm_fill("tasa_white_68"),
  tm_shape(ohlung) + tm_fill("tasa_black_68")
)

tmap_arrange(
  tm_shape(ohlung) + tm_fill("tasa_white_78"),
  tm_shape(ohlung) + tm_fill("tasa_black_78")
)

tmap_arrange(
  tm_shape(ohlung) + tm_fill("tasa_white_88"),
  tm_shape(ohlung) + tm_fill("tasa_black_88")
)
```

Algo notorio que podemos observar, es la tasa constante de las personas negras con cancer de pulmón en el mapa de cada año, es decir, que no hay tanta varianción entre condados, también se ve que las tasas de la población negra son mayores que las tasas de la población blanca, además con el paso de los años aumenta.

En general se ve un aumento en las tasas de cancer de pulmón, más notoriamente en la población de blancos que en la población de negros.

```{r include=FALSE}
ohlung$tasa_mujer_68 <- ohlung$LF68 / ohlung$POPF68
ohlung$tasa_hombre_68 <- ohlung$LM68 / ohlung$POPM68

ohlung$tasa_mujer_78 <- ohlung$LF78 / ohlung$POPF78
ohlung$tasa_hombre_78 <- ohlung$LM78 / ohlung$POPM78

ohlung$tasa_mujer_88 <- ohlung$LF88 / ohlung$POPF88
ohlung$tasa_hombre_88 <- ohlung$LM88 / ohlung$POPM88
```

```{r}
tmap_arrange(
  tm_shape(ohlung) + tm_fill("tasa_mujer_68"),
  tm_shape(ohlung) + tm_fill("tasa_hombre_68")
)

tmap_arrange(
  tm_shape(ohlung) + tm_fill("tasa_mujer_78"),
  tm_shape(ohlung) + tm_fill("tasa_hombre_78")
)

tmap_arrange(
  tm_shape(ohlung) + tm_fill("tasa_mujer_88"),
  tm_shape(ohlung) + tm_fill("tasa_hombre_88")
)
```
Podemos ver que en ambos grupos se presenta variación a través de los condados, además de esto los hombres presentan tasas más altas de cancer de pulmón, las cuales aumentan conforme pasan los años.

Algo que se debe resaltar es que hay un aumento en las tasas tanto de hombres como de mujeres entre 1978 y 1988.

```{r matriz dispersion, echo=FALSE, fig.height=7, fig.width=10, message=FALSE, warning=FALSE}
numericas <- ohlung %>% 
  sf::st_drop_geometry() %>% 
  dplyr::select(-c(COUNTYID,FIPSNO,RECORD_ID,NAME))
cor_numericas <- cor(numericas)

correlacion <- lapply(seq_len(ncol(cor_numericas)), function(i) {
  
  cor_vec <- cor_numericas[i, ]
  
  # quitar autocorrelación
  cor_vec <- cor_vec[-i]
  
  # ordenar
  cor_ordenadas <- sort(cor_vec, decreasing = TRUE)
  
  paste(names(cor_ordenadas), round(cor_ordenadas, 3), sep = ";")
})

# convertir a data.frame
correlacion <- as.data.frame(correlacion)

# nombres de columnas = variables originales
colnames(correlacion) <- colnames(cor_numericas)

kableExtra::kable(correlacion, caption = "Correlación de pearson",
                  digits = 3)
```
Lo que podemos destacar de la tabla es:

* Podemos evidenciar que área y perímetro solo tienen una fuerte correlación lineal entre ellas, mientras que con el resto de las variables (incluyendo la variable respuesta) presentan una correlación baja, por lo que no serán selecionadas para el modelo.

* Las variables explicativas originales presentan alta correlación entre sí, por lo que solo se conservará variables que guarden la información de sexo, y raza.

*Variables explicativas seleccionadas:*

* *LMW68*: Número de casos de cáncer de púlmon en hombres blancos en el año de 1968.

* *LMW78*: Número de casos de cáncer de púlmon en hombres blancos en el año de 1978.

* *LFW68*: Número de casos de cáncer de púlmon en mujeres blancos en el año de 1968.

* *LFW78*: Número de casos de cáncer de púlmon en mujeres blancos en el año de 1978.


# Evaluación y Selección de la Matriz $W$
La elección de $W$ no es arbitraria; se debe seleccionar aquella que maximice la captura de la dependencia espacial en los datos de 1988.


Según el sustento teórico, la matriz de pesos puede basarse en la similitud de una variable $X$ altamente relacionada con la respuesta $Z$. Para esto, definimos $w_{ij} = 1/(x_i - x_j)$, donde $x$ es la variable con mayor correlación con la tasa de cáncer de 1988. 

Se evalúan cuatro tipos de estructuras:

*   **Contigüidad Física:** Rook (Torre) y Queen (Reina).

*   **Basadas en Distancia:** $k$-vecinos más cercanos con $k=3,4,5, 6,7, 8, 9, 10$.

*   **Basada en Atributos:** Ponderación por diferencia de tasas históricas (similitud epidemiológica).

```{r matriz W, include=FALSE}

#- Crea puntos representativos (centroides) 
st_crs(ohlung) <- 26917
xy0 <- st_centroid(ohlung)  
coords <- st_coordinates(xy0) # Extrae coordenadas
head(coords)

#- Grafica el mapa de municipios con los centroides ----
plot(st_geometry(ohlung), border = "grey",
     col = "white", axes = TRUE) # Mapa base de municipios
title(main = "Condados de Ohaio") # Título
plot(st_geometry(xy0), add = TRUE, pch = 19, cex = 0.5) # Centroides encima
grid() # Cuadrícula
 

#Physical neighbours
rook_nb_b <- nb2listw(poly2nb(ohlung,queen=FALSE), 
                      style="B",zero.policy = TRUE)
rook_nb_w <- nb2listw(poly2nb(ohlung,queen=FALSE), 
                      style="W",zero.policy = TRUE)
queen_nb_b <- nb2listw(poly2nb(ohlung,queen=TRUE), 
                       style="B",zero.policy = TRUE)
queen_nb_w <- nb2listw(poly2nb(ohlung,queen=TRUE), 
                       style="W",zero.policy = TRUE)


#- Estructuras de vecindad basadas en distancia y grafos ----
# convertir sf a sp
xy_sp <- as(xy0, "Spatial") 

#- vecinos por triangulación de Delaunay.---- 
#Une puntos cercanos formando triángulos.
trinb <- tri2nb(xy_sp)

# Convierte esa vecindad en pesos:
tri_nb_b <- nb2listw(tri2nb(xy_sp), style = "B", zero.policy = TRUE)
tri_nb_w <- nb2listw(tri2nb(xy_sp), style = "W", zero.policy = TRUE)


#- vecinos con el criterio Sphere of Influence.---- 
#Parte de la triangulación y deja conexiones según cercanía/influencia entre puntos.
soi_nb_b <- nb2listw(graph2nb(soi.graph(trinb, xy_sp)), style = "B", zero.policy = TRUE)
soi_nb_w <- nb2listw(graph2nb(soi.graph(trinb, xy_sp)), style = "W", zero.policy = TRUE)



#- Crea vecinos por vecino relativo.---- 
#Conecta dos puntos si no hay un tercer punto “más cercano” que se interponga.

relative_nb_b <- nb2listw(graph2nb(relativeneigh(xy_sp), sym = TRUE), style = "B", zero.policy = TRUE)
relative_nb_w <- nb2listw(graph2nb(relativeneigh(xy_sp), sym = TRUE), style = "W", zero.policy = TRUE)


##- Crea vecinos por grafo de Gabriel. ---- 
# Conecta dos puntos si el círculo entre ellos no contiene otros puntos.
gabriel_nb_b <- nb2listw(graph2nb(gabrielneigh(xy_sp), sym = TRUE), style = "B", zero.policy = TRUE)
gabriel_nb_w <- nb2listw(graph2nb(gabrielneigh(xy_sp), sym = TRUE), style = "W", zero.policy = TRUE)

##- Graficos vecindad basadas en distancia y grafos ----
# Crear objetos nb
trinb <- tri2nb(xy_sp)
soinb <- graph2nb(soi.graph(trinb, xy_sp))
gabrielnb <- graph2nb(gabrielneigh(xy_sp), sym = TRUE)
relativenb <- graph2nb(relativeneigh(xy_sp), sym = TRUE)

plot(st_geometry(ohlung), border = "gray")
plot(trinb, coords, add = TRUE, col = "red")
title("Delaunay")

plot(st_geometry(ohlung), border = "gray")
plot(soinb, coords, add = TRUE, col = "green")
title("Sphere of Influence")

plot(st_geometry(ohlung), border = "gray")
plot(gabrielnb, coords, add = TRUE, col = "blue")
title("Gabriel")

plot(st_geometry(ohlung), border = "gray")
plot(relativenb, coords, add = TRUE, col = "orange")
title("Relative Neighbour")

#- W por k vecinos más cercanos ----

knn1_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 1)), style = "B", zero.policy = TRUE)
knn1_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 1)), style = "W", zero.policy = TRUE)

knn2_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 2)), style = "B", zero.policy = TRUE)
knn2_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 2)), style = "W", zero.policy = TRUE)

knn3_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 3)), style = "B", zero.policy = TRUE)
knn3_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 3)), style = "W", zero.policy = TRUE)

knn4_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 4)), style = "B", zero.policy = TRUE)
knn4_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 4)), style = "W", zero.policy = TRUE)

knn5_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 5)), style = "B", zero.policy = TRUE)
knn5_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 5)), style = "W", zero.policy = TRUE)

knn6_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 6)), style = "B", zero.policy = TRUE)
knn6_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 6)), style = "W", zero.policy = TRUE)

knn7_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 7)), style = "B", zero.policy = TRUE)
knn7_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 7)), style = "W", zero.policy = TRUE)

knn8_nb_b <- nb2listw(knn2nb(knearneigh(xy_sp, k = 8)), style = "B", zero.policy = TRUE)
knn8_nb_w <- nb2listw(knn2nb(knearneigh(xy_sp, k = 8)), style = "W", zero.policy = TRUE)


plot(st_geometry(ohlung), border = "gray")
plot(knn1_nb_w, coords, add = TRUE, col = "red")
title("k = 1")

plot(st_geometry(ohlung), border = "gray")
plot(knn2_nb_w, coords, add = TRUE, col = "blue")
title("k = 2")

plot(st_geometry(ohlung), border = "gray")
plot(knn3_nb_w, coords, add = TRUE, col = "green")
title("k = 3")

plot(st_geometry(ohlung), border = "gray")
plot(knn4_nb_w, coords, add = TRUE, col = "orange")
title("k = 4")

plot(st_geometry(ohlung), border = "gray")
plot(knn5_nb_w, coords, add = TRUE, col = "red")
title("k = 5")

plot(st_geometry(ohlung), border = "gray")
plot(knn6_nb_w, coords, add = TRUE, col = "blue")
title("k = 6")

plot(st_geometry(ohlung), border = "gray")
plot(knn7_nb_w, coords, add = TRUE, col = "green")
title("k = 7")

plot(st_geometry(ohlung), border = "gray")
plot(knn8_nb_w, coords, add = TRUE, col = "orange")
title("k = 8")


#----------------------------------------------------------------

#- Evaluación y selección de matrices de pesos espaciales ----

# Crea lista de todas las matrices creadas
mat <- list(
  rook_nb_b, rook_nb_w,
  queen_nb_b, queen_nb_w,
  tri_nb_b, tri_nb_w,
  soi_nb_b, soi_nb_w,
  gabriel_nb_b, gabriel_nb_w,
  relative_nb_b, relative_nb_w,
  knn1_nb_w, knn1_nb_b,
  knn2_nb_w, knn2_nb_b,
  knn3_nb_w, knn3_nb_b,
  knn4_nb_w, knn4_nb_b,
  knn5_nb_w, knn5_nb_b,
  knn6_nb_w, knn6_nb_b,
  knn7_nb_w, knn7_nb_b,
  knn8_nb_w, knn8_nb_b
)


# Nombre a cada elemto de la fila
names(mat) <- c(
  "rook_b", "rook_w",
  "queen_b", "queen_w",
  "tri_b", "tri_w",
  "soi_b", "soi_w",
  "gabriel_b", "gabriel_w",
  "relative_b", "relative_w",
  "knn1_W", "knn1_b",
  "knn2_W", "knn2_b",
  "knn3_W", "knn3_b",
  "knn4_W", "knn4_b",
  "knn5_W", "knn5_b",
  "knn6_W", "knn6_b",
  "knn7_W", "knn7_b",
  "knn8_W", "knn8_b"
)

aux_p <- numeric(length(mat)) # Vector numérico vacío para los p-values 
aux_I <- numeric(length(mat)) # Vector numérico vacío para los indiced e moran 

# Recorre TODAS las matrices W y calcula Moran para cada una.
for(i in seq_along(mat)){
  test <- moran.test(ohlung$tasa_88, mat[[i]], alternative = "two.sided")
  
  aux_p[i] <- test$p.value
  aux_I[i] <- test$estimate[1]   # Moran's I
}



# Guardar el valor-p de cada matriz ordenado de menor a mayor
resultados_w <- data.frame(
  matriz = names(mat),
  moran_I = aux_I,
  p_value = aux_p
)


kableExtra::kable(resultados_w[order(resultados_w$p_value), ],
      digits = 3)


```
De acuerdo con la tabla, podemos ver en la mayoría (24 de 28) el valor p es menor al nivel de significancia ($\alpha=0.05$), por lo cual se rechaza la hipotesis nula de independencia espacial, lo cual evidencia correlación espacial en los datos, teniendo en cuenta que hubo casos específicos en los que no se rechaza esa hipótesis nula, puede entenderse como que no en todas las configuraciones la dependencia espacial es consistente.

Por otro lado como en este caso estamos manejando condados (regiones) se deicidió optar por usar la matriz de pesos de vecindad por el criterio reina, ya que entre la matriz reina y torre, con pesos y binarias (en cada caso), la matriz de reina con pesos es la que tiene menor p valor.

```{r mapa_lisa, message=FALSE, warning=FALSE}

# 1. Calculamos el Moran Local usando la matriz ganadora (KNN7)
local_moran <- localmoran(ohlung$tasa_88, knn8_nb_w, zero.policy = TRUE)

# Dividimos el área gráfica en 1 fila y 2 columnas
grid.newpage()

pushViewport(viewport(layout = grid.layout(1, 2)))

print_moran_plot <- function() {
  par(mar = c(5, 4, 4, 1), new = FALSE)
  mp <- moran.plot(ohlung$tasa_88, knn8_nb_w, 
         labels = as.character(ohlung$NAME), 
         pch = 19, 
         col = "black",          # Puntos normales en negro
         bg = "red",             # Puntos influyentes en rojo
         xlab = "Tasa de Cáncer 1988", 
         ylab = "Tasa Rezagada (Promedio Vecinos KNN8)",
         main = "Dispersograma de Moran: Cáncer de Pulmón en Ohio (1988)",
         las = 1, 
         cex = 0.7)

# Usamos 'bg' para resaltar los resultados estadísticos
legend("bottomright", 
       legend = c("I de Moran: 0.1548", 
                  "Valor P:    0.00065",
                  "Matriz:     KNN8"), 
       cex = 0.9,
       bg = "#D5F5E3", # Un verde claro profesional
       box.col = "darkgreen",
       inset = 0.02)
grid(col = "lightgray", lty = "dotted")
mtext("Cuadrantes: Superior-Derecha (Alto-Alto), Inferior-Izquierda (Bajo-Bajo)", 
      side = 3, line = -1.0, cex = 0.7, adj = 0.02, col = "darkblue")
}

# Dibujar el primer gráfico en la columna 1
print(print_moran_plot(), vp = viewport(layout.pos.row = 1, layout.pos.col = 1))

# 2. Preparar los datos para el mapeo
ohlung$moran_i <- local_moran[,1]     # Índice de Moran local
ohlung$p_valor <- local_moran[,5]     # P-valor del test local

# 3. Escalar la variable para identificar los cuadrantes (Alto-Alto, Bajo-Bajo, etc.)
tasa_scale <- scale(ohlung$tasa_88)
lag_scale <- scale(lag.listw(knn8_nb_w, ohlung$tasa_88))

# 4. Crear las categorías de los clústeres
ohlung$cluster <- "No Significativo"
ohlung$cluster[tasa_scale > 0 & lag_scale > 0 & ohlung$p_valor < 0.05] <- "Alto-Alto (Hotspot)"
ohlung$cluster[tasa_scale < 0 & lag_scale < 0 & ohlung$p_valor < 0.05] <- "Bajo-Bajo (Coldspot)"
ohlung$cluster[tasa_scale > 0 & lag_scale < 0 & ohlung$p_valor < 0.05] <- "Alto-Bajo (Atípico)"
ohlung$cluster[tasa_scale < 0 & lag_scale > 0 & ohlung$p_valor < 0.05] <- "Bajo-Alto (Atípico)"


# Primero preparamos una columna de etiquetas solo para los significativos
ohlung$etiqueta_lisa <- ifelse(ohlung$cluster == "No Significativo", "", as.character(ohlung$NAME))


mapa_lisa <- tm_shape(ohlung) +
  tm_polygons("cluster", 
              title = "Clústeres LISA (Tasa 88)",
              palette = c("Alto-Alto (Hotspot)" = "#e31a1c", 
                          "Bajo-Bajo (Coldspot)" = "#1f78b4", 
                          "Alto-Bajo (Atípico)" = "#a6cee3", 
                          "Bajo-Alto (Atípico)" = "#fb9a99", 
                          "No Significativo" = "#f0f0f0"),
              border.col = "grey40",
              lwd = 0.4) +
  # Añadimos los nombres destacados
  tm_text("etiqueta_lisa", size = 0.6, col = "black", remove.overlap = TRUE) +
  tm_layout(main.title = "B. Mapa de Clústeres (LISA)",
            main.title.size = 1,
            legend.position = c("left", "bottom"),
            legend.title.size = 0.8,
            legend.text.size = 0.6,
            frame = FALSE)

# Dibujar el mapa en la columna 2
print(mapa_lisa, vp = viewport(layout.pos.row = 1, layout.pos.col = 2))


# 
# # 5. Dibujar el mapa con tmap
# tm_shape(ohlung) +
#   tm_polygons("cluster",
#               title = "Clústeres LISA (Tasa 88)",
#               palette = c("Alto-Alto (Hotspot)" = "#FF0000",
#                          "Bajo-Bajo (Coldspot)" = "#2F00FF",
#                          "Alto-Bajo (Atípico)" = "#D24AF7",
#                          "Bajo-Alto (Atípico)" = "#9900FF",
#                          "No Significativo" = "white"),
#               border.col = "black",
#               lwd = 0.5) +
#   tm_layout(main.title = "Mapa de Clústeres de Cáncer de Pulmón",
#             legend.outside = TRUE)
```

El patrón espacial es heterogéneo. Tienes un gradiente claro: un noroeste "sano" (azul) frente a un sur y sureste con focos críticos (rojo). 


## 1. Comparación Estructural y Topológica
Antes de modelar, es vital entender cómo "se ven" las conexiones espaciales. Una matriz de pesos espaciales ($W$) captura el arreglo geométrico de los valores en el mapa. Compararemos la matriz **KNN8** (seleccionada previamente) con una de contigüidad **Reina** para evaluar la densidad de la red.

```{r comparacion_estructural}
# 2. Visualización Topológica
par(mfrow = c(1, 2), mar = c(0, 0, 1, 0))
plot(st_geometry(ohlung), border = "grey", main = "Contigüidad Reina")
plot(queen_nb_w, coords, add = TRUE, col = "blue", lwd = 0.5)

plot(st_geometry(ohlung), border = "grey", main = "K-Vecinos (k=8)")
plot(knn8_nb_w, coords, add = TRUE, col = "red", lwd = 0.5)

# # 3. Métricas de conectividad
# cat("Promedio de vecinos Reina:", mean(card(nb_q)), "\n")
# cat("Promedio de vecinos KNN8:", mean(card(knn8_nb_w$neighbours)), "\n")
```

## 2. Diagrama de Dispersión de Moran (Moran Scatterplot)
Este gráfico visualiza la relación entre el valor de la tasa en un condado ($z_i$) y el promedio ponderado de sus vecinos ($\sum c_{ij}z_j$).
*   **Cuadrantes I y III:** Representan autocorrelación espacial positiva (Hotspots y Coldspots).
*   **Cuadrantes II y IV:** Representan autocorrelación negativa (Atípicos espaciales).

```{r moran_scatterplot}
# Nota: moran.plot dibuja automáticamente las líneas de los promedios (cuadrantes)
moran.plot(ohlung$tasa_88, knn8_nb_w, 
         labels = as.character(ohlung$NAME), 
         pch = 19, 
         col = "black",          # Puntos normales en negro
         bg = "red",             # Puntos influyentes en rojo
         xlab = "Tasa de Cáncer 1988", 
         ylab = "Tasa Rezagada (Promedio Vecinos KNN8)",
         main = "Dispersograma de Moran: Cáncer de Pulmón en Ohio (1988)",
         las = 1, 
         cex = 0.7)

# Usamos 'bg' para resaltar los resultados estadísticos
legend("bottomright", 
       legend = c("I de Moran: 0.1548", 
                  "Valor P:    0.00065",
                  "Matriz:     KNN8"), 
       cex = 0.9,
       bg = "#D5F5E3", # Un verde claro profesional
       box.col = "darkgreen",
       inset = 0.02)
grid(col = "lightgray", lty = "dotted")
mtext("Cuadrantes: Superior-Derecha (Alto-Alto), Inferior-Izquierda (Bajo-Bajo)", 
      side = 3, line = -1.0, cex = 0.7, adj = 0.02, col = "darkblue")
```

El diagrama de dispersión de Moran muestra una relación lineal positiva, ratificando la existencia de autocorrelación espacial. Se identifican condados como Monroe y Jefferson en el cuadrante Alto-Alto, actuando como núcleos de los clústeres de alta incidencia. Por el contrario, condados como Noble actúan como outliers espaciales, presentando tasas significativamente menores que el promedio de sus vecinos.

## 3. Exploración de Promedios Móviles Espaciales
La matriz $W$ actúa como un **operador de rezago espacial**. Al calcular el promedio ponderado de los vecinos, suavizamos la variable para detectar tendencias regionales de largo alcance en lugar de ruido local.

```{r promedios_moviles, message=FALSE, warning=FALSE}
# Calcular el rezago espacial (promedio móvil)
ohlung$tasa_lag88 <- lag.listw(knn8_nb_w, ohlung$tasa_88)

# Comparar mapa original vs suavizado
tmap_arrange(
  tm_shape(ohlung) + tm_polygons("tasa_88", title = "Tasa Original", palette = "Reds"),
  tm_shape(ohlung) + tm_polygons("tasa_lag88", title = "Promedio Móvil (KNN8)", palette = "Reds")
)
```




# Regresión poisson

Debido a la naturaleza de los datos, al tratarse de conteos o tasas de incidencia decidimos manejar una regresión poisson para modelar los datos.

## glm1
```{r glm1, echo=FALSE, fig.height=10, fig.width=10, warning=FALSE}
glm1 <- glm(casos_totales88~LMW68+LMW78+LFW68+LFW78+
              offset(log(pop_total88)),
            data=ohlung,family = poisson(link = "log"))
summary(glm1)
check_model(glm1)
```
Observamos que la variable *PERIMETER* no fue significativa, de igual manera parece ser que la raza tampoco es significativa.

Por otro lado parece ser que por condado hay correlación perfecta.

Los residuales presentan un comportamiento creciente y más disperso, lo que indica un patrón no explicado y sobre dispersión.

Hubo observaciones influyentes, sin embargo no son demasiadas.

## *Independecia*

```{r acf glm1}
acf(residuals.glm(glm1), main = "ACF de los residuales", col = "blue", lwd = 2)

```

El correlograma muestra que no parece haber una correlación fuerte en los residuales, sin embargo, se decidió hacer una verificación más formal del supuesto de independencia en los errores, usando la prueba de Durbin Watson

```{r independencia glm1}
dwtest(glm1)
```
Debido a que el p valor fue mayor al nivel de significancia ($\alpha=0.05$), se rechaza la $H_0:$ independencia, por lo que no hay evidencia suficiente estadística para creer que los errores no estan correlacionados o son independientes, en pocas palabras se rechaza la independencia.

```{r}
moran.test(residuals.glm(glm1), listw = queen_nb_w)
moran.test(residuals.glm(glm1), listw = knn8_nb_w)
```

## *Normalidad*

```{r graficos normales glm1}
hist(residuals.glm(glm1),breaks = 50, main = "Histograma de los residuales", xlab = "Residuos", ylab = "Frecuencia", col = "lightblue", border = "white")
qqnorm(residuals.glm(glm1), main = "QQ-Plot de los residuales")
qqline(residuals.glm(glm1), col = "red", lwd = 2)
```

En el histograma no podemos ver claramente una distribución simétrica y centrada en 0, pero en el qqplot podemos ver que las observaciones estan alrededor de la línea.

Para confirmar la sospecha de normalidad se decidió hacer la prueba de $Shapiro-Wilk$ 

```{r normalidad prueba glm1}
shapiro.test(residuals.glm(glm1))
```
De este modo confirmamos que la normalidad no se rechaza con un p valor mayor al nivel de significancia ($\alpha=0.05$).


## *Homocedasticidad*

Para evaluar la heteroscedasticidad formalmente, tomando en cuenta que la población no sigue o proviene de una distrincuón normal, se decidió aplicar el test de Breusch-pagan.

```{r breusch-pagan glm1, echo=FALSE, message=FALSE, warning=FALSE}
bptest(glm1)
```

Con un p valor menor al nivel de significancia (0.05) se rechaza la $H_0$ de igualdad de varianzas, es algo que ya se podría detectar a simple vista en el gráfico de los residuales y los valores predichos.


## Quasi poisson

Tratando de manejar la sobredispersión se intentó realizar un modelo quasi poisson.

```{r quasi poisson, fig.height=10, fig.width=10}
glm_qp <- glm(casos_totales88~LMW68+LMW78+LFW68+LFW78+
              offset(log(pop_total88)),
              data = ohlung)
summary(glm_qp)
check_model(glm_qp)
```

Las estimaciones son prácticamente las mismas del modelo *glm2*, pero así mismo presentar el mismo problema de los residuales de este, excepto que en este los residuales no tienen ni si quiera el comportamiento de una distribución normal.

## *Independecia*

```{r acf glm_qp}
acf(residuals.glm(glm_qp), main = "ACF de los residuales", col = "blue", lwd = 2)

```

El correlograma muestra que no parece haber una correlación fuerte en los residuales, sin embargo, se decidió hacer una verificación más formal del supuesto de independencia en los errores, usando la prueba de Durbin Watson

```{r independencia glm_qp}
dwtest(glm_qp)
```
Debido a que el p valor fue mayor al nivel de significancia ($\alpha=0.05$), se rechaza la $H_0:$ independencia, por lo que no hay evidencia suficiente estadística para creer que los errores no estan correlacionados o son independientes, en pocas palabras se rechaza la independencia.


```{r}
moran.test(residuals.glm(glm_qp), listw = queen_nb_w)
moran.test(residuals.glm(glm_qp), listw = knn8_nb_w)
```

## *Normalidad*

```{r graficos normales glm_qp}
hist(residuals.glm(glm_qp),breaks = 50, main = "Histograma de los residuales", xlab = "Residuos", ylab = "Frecuencia", col = "lightblue", border = "white")
qqnorm(residuals.glm(glm_qp), main = "QQ-Plot de los residuales")
qqline(residuals.glm(glm_qp), col = "red", lwd = 2)
```

En el histograma podemos ver un ligero sesgo a derecha, y en el qqplot podemos ver que las observaciones no estan alrededor de la línea y que hay problemas bastante evidentes en las colas.

Para confirmar la sospecha de no normalidad se decidió hacer la prueba de $Shapiro-Wilk$ 

```{r normalidad prueba glm_qp}
shapiro.test(residuals.glm(glm_qp))
```
De este modo confirmamos que la normalidad se rechaza con un p valor menor al nivel de significancia ($\alpha=0.05$).


## *Homocedasticidad*

Para evaluar la heteroscedasticidad formalmente, tomando en cuenta que la población no sigue o proviene de una distrincuón normal, se decidió aplicar el test de Breusch-pagan.

```{r breusch-pagan glm_qp, echo=FALSE, message=FALSE, warning=FALSE}
bptest(glm_qp)

```

Con un p valor menor al nivel de significancia (0.05) se rechaza la $H_0$ igualdad de varianzas, es algo que ya se podría detectar a simple vista en el gráfico de los residuales y los valores predichos.


## Doblemente generalizado

```{r doblemente generalizado, fig.height=10, fig.width=10}

dglm_model <- dglm(casos_totales88~LMW68+LMW78+LFW68+LFW78,
                   ~ cambio_68_78,          
                   offset = log(pop_total88),
                   family = poisson(link = "log"),
                   dfamily = Gamma(link = "log"),   
                   data = ohlung)

summary(dglm_model)
check_model(dglm_model)
```

En este ninguna variable se mostró como significativa y de hecho se vieron más observaciones influyentes, el comportamiento de los residuales no es para nada normal y sigue existiendo el problema del 0 inflado.

### Modelo de media

#### *Independecia*

```{r acf dglm_media}
acf(residuals(dglm_model), main = "ACF de los residuales", col = "blue", lwd = 2)

```

Se ve una clara alta correlación entre los residuales, pero para tener una confirmación más formal se decidió aplicar una prueba de $Durbin-Watson$.

```{r independencia dglm_media}
dwtest(dglm_model)
```
En efecto se rechaza la independencia de los errores, con un p valor menor al nivel de significancia.

```{r}
moran.test(residuals(dglm_model), listw = queen_nb_w)
moran.test(residuals(dglm_model), listw = knn8_nb_w)
```

#### *Normalidad*

```{r graficos normales dglm_media}
hist(residuals(dglm_model),breaks = 50, main = "Histograma de los residuales", xlab = "Residuos", ylab = "Frecuencia", col = "lightblue", border = "white")
qqnorm(residuals(dglm_model), main = "QQ-Plot de los residuales")
qqline(residuals(dglm_model), col = "red", lwd = 2)
```

Se ve claramente en el histograma un sesgo a derecha, y en el qqplot podemos observar una evidencia fuerte tambien de no normalidad sobre todo por las colas.

Para confirmar la sospecha de no normalidad se decidió hacer la prueba de $Shapiro-Wilk$ 

```{r normalidad prueba dglm_media}
shapiro.test(residuals(dglm_model))
```
Se confirma la no normalidad de los errores con un p valor menor al nivel de significancia.

#### *Homocedasticidad*

```{r grafico dispersion dglm_media, echo=FALSE, message=FALSE, warning=FALSE}
plot(fitted(dglm_model),
     residuals(dglm_model,type="pearson"))
abline(h=0,col="red")

```

Podemos notar que se presenta una varianza creciente con una forma notoria en los residuales de cono.

### Modelo de dispersión

#### *Independecia*

```{r acf dglm_varianza}
acf(residuals(dglm_model$dispersion.fit), main = "ACF de los residuales", col = "blue", lwd = 2)

```

Se ve una clara alta correlación entre los residuales, pero para tener una confirmación más formal se decidió aplicar una prueba de $Durbin-Watson$.

```{r independencia dglm_varianza}
dwtest(dglm_model$dispersion.fit)
```
En efecto se rechaza la independencia de los errores, con un p valor menor al nivel de significancia.

```{r}
moran.test(residuals(dglm_model$dispersion.fit), listw = queen_nb_w)
moran.test(residuals(dglm_model$dispersion.fit), listw = knn8_nb_w)
```

#### *Normalidad*

```{r graficos normales dglm_varianza}
hist(residuals(dglm_model$dispersion.fit),breaks = 50, main = "Histograma de los residuales", xlab = "Residuos", ylab = "Frecuencia", col = "lightblue", border = "white")
qqnorm(residuals(dglm_model$dispersion.fit), main = "QQ-Plot de los residuales")
qqline(residuals(dglm_model$dispersion.fit), col = "red", lwd = 2)
```

Se ve en el histograma un comportamiento bimodal, y en el qqplot podemos observar una evidencia fuerte tambien de no normalidad sobre todo por las colas.

Para confirmar la sospecha de no normalidad se decidió hacer la prueba de $Shapiro-Wilk$ 

```{r normalidad prueba dglm_dispersion}
shapiro.test(residuals(dglm_model$dispersion.fit))
```
Se confirma la no normalidad de los errores con un p valor menor al nivel de significancia.


#### *Homocedasticidad*

```{r grafico dispersion dglm_varianza, echo=FALSE, message=FALSE, warning=FALSE}
plot(fitted(dglm_model$dispersion.fit),
     residuals(dglm_model$dispersion.fit))
abline(h=0,col="red")

```


## Efecto aleatorio

Para controlar la sobre dispersión se decidió usar un efecto aleatorio por condado asumiendo que dentro de cada condado hay características diferentes no controlables, como la contaminación ambiental, recursos económicos o políticas de salud, entre otros.

```{r efecto aleatorio, fig.height=10, fig.width=10}
# Modelo poisson con efecto aleatorio por condado
modelo_glmer <- glmer(casos_totales88~LMW68+LMW78+LFW68+LFW78+(1 | NAME) + 
                        offset(log(pop_total88)),
                      family = poisson(link = "log"),
                      data = ohlung)

summary(modelo_glmer)
plot(modelo_glmer)
check_model(modelo_glmer)
```

Nuevamente raza deja de ser significativo.

Los residuales parecen estar centrados, sin embargo todavía parecen presentar problemas de heteroscedasticidad, aunque parece haber mejorado un poco el problema de los 0's inflados, todavía parece haber un poco de sobredispersión.

Los efectos aleatorios tambien parecen cumplir con los supuestos.

Se realizaron pruebas para verificar los resultados de las gráficas.

### *Independencia:*
```{r acf efecto aleatorio, echo=FALSE, fig.height=5, fig.width=10}
acf(residuals(modelo_glmer))
```

Parecen haber algunas correlaciones entre los residuales.

```{r  residuales efecto aleatorio, fig.height=7, fig.width=10}
sim <- simulateResiduals(modelo_glmer)
plot(sim)
testResiduals(sim)
```

Se decidió realizar una prueba de rachas 


```{r rachas efecto aleatorio}
runs.test(factor(residuals(modelo_glmer) > 0))
```
Como el p valor obtuvo un valor mayor al nivel de significancia, no se rechaza $H_0:$ los residuales son independientes.

### *Normalidad*

```{r graficos normales efecto aleatorio, fig.height=7, fig.width=10}
hist(residuals(modelo_glmer),breaks = 50, main = "Histograma de los residuales", xlab = "Residuos", ylab = "Frecuencia", col = "lightblue", border = "white")
qqnorm(residuals(modelo_glmer), main = "QQ-Plot de los residuales")
qqline(residuals(modelo_glmer), col = "red", lwd = 2)
```

Se ve en el histograma un sesgamiento a derecha, y en el qqplot podemos observar una evidencia fuerte tambien de no normalidad sobre todo por las colas.

Para confirmar la sospecha de no normalidad se decidió hacer la prueba de $Shapiro-Wilk$ 

```{r normalidad prueba efecto aleatorio}
shapiro.test(residuals(modelo_glmer))
```
Como el p valor es menor al nivel de significancia ($\alpha=0.05$), se rechaza $H_0:$ los residuales provienen de una población normal.

### *Homocedasticidad*


```{r breusch-pagan efecto aleatorio, echo=FALSE, message=FALSE, warning=FALSE}
sim_res <- simulateResiduals(modelo_glmer)
plot(sim_res)

```

# Regresión binomial negativa

## bnglm

Para enfrentar los problemas de sobre dispersión se pensó en realizar un modelo con binomial negativa.
```{r binomial negativa, fig.height=10, fig.width=10}
bnglm <- glm.nb(formula = casos_totales88~LMW68+LMW78+LFW68+LFW78+
               offset(log(pop_total88)),
            data=ohlung)
summary(bnglm)
check_model(bnglm)
# envelope(bnglm, rep=500)
```

Se destaca que el hecho de que las estimaciones son del mismo signo que en *glm2*, tambien hubo algunas que estaban bastente cercanas en número. Nuevamente todas las variables fueron significativas, pero el comportamiento de los residuales sigue viendose casi del mismo modo que en los modelos *glm1* y *glm2*.

### *Independecia*

```{r acf bnglm}
acf(residuals(bnglm), main = "ACF de los residuales", col = "blue", lwd = 2)

```

El correlograma muestra una leve señal de correlación en los residuales, sin embargo, se decidió hacer una verificación más formal del supuesto de independencia en los errores, usando la prueba de Durbin Watson

```{r independencia bnglm}
dwtest(bnglm)
```
Debido a que el p valor fue mayor al nivel de significancia ($\alpha=0.05$), se rechaza la $H_0:$ independencia, por lo que no hay evidencia suficiente estadística para creer que los errores no estan correlacionados o son independientes, en pocas palabras se rechaza la independencia.

```{r}
moran.test(residuals(bnglm), listw = queen_nb_w)
moran.test(residuals(bnglm), listw = knn8_nb_w)
```

### *Normalidad*

```{r graficos normales bnglm}
hist(residuals(bnglm),breaks = 50, main = "Histograma de los residuales", xlab = "Residuos", ylab = "Frecuencia", col = "lightblue", border = "white")
qqnorm(residuals(bnglm), main = "QQ-Plot de los residuales")
qqline(residuals(bnglm), col = "red", lwd = 2)
```

En el histograma podemos ver aproximadamente una distribución simétrica alrededor entre -1 y 0, y en el qqplot podemos ver que las observaciones no estan alrededor de la línea y que hay problemas bastante evidentes en las colas.

Para confirmar la sospecha de no normalidad se decidió hacer la prueba de $Shapiro-Wilk$ 

```{r normalidad prueba bnglm}
shapiro.test(residuals(bnglm))
```
De este modo confirmamos que la normalidad se rechaza con un p valor menor al nivel de significancia ($\alpha=0.05$)


### *Homocedasticidad*

Para evaluar la heteroscedasticidad formalmente, tomando en cuenta que la población no sigue o proviene de una distrincuón normal, se decidió aplicar el test de Breusch-pagan.

```{r breusch-pagan bnglm, echo=FALSE, message=FALSE, warning=FALSE}
bptest(bnglm)

```

Con un p valor menor al nivel de significancia (0.05) se rechaza la $H_0$ igualdad de varianzas, es algo que ya se podría detectar a simple vista en el gráfico de los residuales y los valores predichos.


## Efecto aleatorio

```{r binomial negativa efecto aleatorio, fig.height=10, fig.width=10}
# Modelo con binomial negativa + efecto aleatorio por condado
nbea <- glmmTMB(casos_totales88~LMW68+LMW78+LFW68+LFW78+
                (1 | NAME) + 
                      offset(log(pop_total88)),
                     family = nbinom2,          # ← aquí está el cambio clave
                     data = ohlung)

summary(nbea)
plot(fitted.values(nbea),residuals(nbea))
check_model(nbea)
```

### *Independencia:*
```{r acf bn efecto aleatorio, echo=FALSE, fig.height=5, fig.width=10}
acf(residuals(nbea))
```

Parecen haber algunas correlaciones entre los residuales.

```{r bn residuales efecto aleatorio, fig.height=7, fig.width=10}
sim <- simulateResiduals(nbea)
plot(sim)
testResiduals(sim)
```

Se decidió realizar una prueba de rachas 


```{r bn rachas efecto aleatorio}
runs.test(factor(residuals(nbea) > 0))
```
Como el p valor obtuvo un valor mayor al nivel de significancia, no se rechaza $H_0:$ los residuales son independientes.

```{r}
moran.test(residuals(nbea), listw = queen_nb_w)
moran.test(residuals(nbea), listw = knn8_nb_w)
```

### *Normalidad*

```{r graficos normales bn efecto aleatorio, fig.height=7, fig.width=10}
hist(residuals(nbea),breaks = 50, main = "Histograma de los residuales", xlab = "Residuos", ylab = "Frecuencia", col = "lightblue", border = "white")
qqnorm(residuals(nbea), main = "QQ-Plot de los residuales")
qqline(residuals(nbea), col = "red", lwd = 2)
```

Se ve centralidad alrededor de 0 y una distribución simétrica, y en el qqplot podemos observar una evidencia fuerte tambien de no normalidad sobre todo por las colas.

Para confirmar la sospecha de no normalidad se decidió hacer la prueba de $Shapiro-Wilk$ 

```{r normalidad prueba nbea}
shapiro.test(residuals(nbea))
```
Como el p valor es menor al nivel de significancia ($\alpha=0.05$), se rechaza $H_0:$ los residuales provienen de una población normal.

### *Homocedasticidad*


```{r homo bn efecto aleatorio, echo=FALSE, message=FALSE, warning=FALSE}
sim_res <- simulateResiduals(nbea)
plot(sim_res)

```

## 4. Análisis de Eigenvectores (MESF/MEM)
La técnica **MESF** filtra la autocorrelación de los residuales y la transfiere al intercepto del modelo. Los eigenvectores representan patrones de mapas: los de valores propios altos muestran gradientes globales, mientras que los bajos muestran contrastes locales.

```{r mesf_analysis, message=FALSE, warning=FALSE}
# 1. Matriz de conectividad binaria C
C_mat <- listw2mat(knn8_nb_w)

# 2. Operador de proyección de Moran (M)
n <- nrow(ohlung)
I <- diag(n)
M <- I - matrix(1, n, n)/n
MBM <- M %*% C_mat %*% M

# 3. Eigendecomposición
eig <- eigen(MBM, symmetric = TRUE)
# Filtrar eigenvectores con MC/MCmax > 0.25 (umbral Griffith)
umbral <- 0.25
EV_validos <- as.data.frame(eig$vectors[, eig$values / max(eig$values) > umbral])
colnames(EV_validos) <- paste0("EV", 1:ncol(EV_validos))

# Visualizar el primer eigenvector (Patrón de máxima autocorrelación positiva)
ohlung$EV1 <- EV_validos$EV1
tmap::tmap_mode("plot")
tm_shape(ohlung) + tm_polygons("EV1", palette = "RdYlGn", title = "Eigenvector 1 (Gradiente Principal)")
```

## 5. Pruebas de Diagnóstico para Selección del Modelo (LM Tests)
Para decidir si la dependencia espacial debe modelarse en la **variable dependiente** (Modelo de Rezago/Lag) o en los **términos de error** (Modelo de Error), utilizamos los Tests del Multiplicador de Lagrange sobre un modelo base de Mínimos Cuadrados.

```{r lm_tests, eval=FALSE, include=FALSE}
# 1. Cargamos la librería para la selección
library(MASS)
library(glmnet)


# 2. Preparar los datos (Lasso requiere matrices, no data.frames)
# Seleccionamos las columnas numéricas que quieres evaluar
columnas_x <- c("AREA", "PERIMETER", "tasa_white_68", "tasa_black_68", "tasa_white_78", 
                "tasa_black_78", "tasa_mujer_68", "tasa_hombre_68", "tasa_mujer_78", 
                "tasa_hombre_78", "casos_totales68", "casos_totales78", 
                "pop_total68", "pop_total78", "tasa_68", "tasa_78", 
                "cambio_68_78")

# Creamos la matriz X (predictores) y el vector Y (respuesta)
# Eliminamos filas con NA si las hubiera
datos_limpios <- na.omit(st_drop_geometry(ohlung)[, c("tasa_88", columnas_x)])
x <- as.matrix(datos_limpios[, -1])
y <- datos_limpios$tasa_88

# 3. Ajustar el modelo Lasso con Validación Cruzada para encontrar el mejor 'lambda'
lasso_cv <- cv.glmnet(x, y, alpha = 1) # alpha = 1 es Lasso

# 4. Ver los coeficientes seleccionados
# Buscamos el lambda que da el menor error
coeficientes_finales <- coef(lasso_cv, s = "lambda.min")
print(coeficientes_finales)

# 5. Visualizar la importancia de las variables
plot(lasso_cv)




# 1. Asegurarnos de usar la nueva función lm.RStests
diagnostico_espacial <- lm.RStests(mco_final, knn8_nb_w, test = "all", zero.policy = TRUE)

# 2. Mostrar los resultados
print(diagnostico_espacial)

```

Este resultado es muy interesante porque contradice lo que parecía un modelo "limpio" y revela una estructura espacial oculta que solo las pruebas robustas logran detectar.

### Las Pruebas Simples (RSerr y RSlag)

* RSerr (Error): Tiene un p-valor de 0.7663. No es significativo.

* RSlag (Lag): Tiene un p-valor de 0.0622. Está muy cerca del límite (0.05), pero técnicamente no es significativo al 5%.


### Las Pruebas Robustas (¡Las que mandan aquí!)
Cuando las pruebas simples fallan o son dudosas, miramos las ajustadas/robustas (adjRS). Estas pruebas controlan por la posible presencia de ambos tipos de dependencia espacial al mismo tiempo.

* adjRSerr (p = 0.0060): Muy significativo.
* adjRSlag (p = 0.0009): Extremadamente significativo.

A veces, un tipo de dependencia espacial (lag) "enmascara" a la otra (error). Las pruebas robustas limpian ese efecto mutuo.

Para elegir el modelo seguimos la regla de decisión de Anselin:

  Si ambas pruebas robustas son significativas (como ocurre aquí), elegimos la que tenga el p-valor más pequeño o el estadístico más alto: adjRSlag ($p = 0.0009$) es mucho más pequeño que adjRSerr ($p = 0.0060$)  un Modelo de Retardo Espacial (Spatial Lag Model - SAR).
  
Esto indica que, incluso después de controlar por población, raza y tasas históricas, la tasa de cáncer de un municipio sí está influenciada directamente por las tasas de sus vecinos. Existe un efecto de "derrame" o contagio espacial (spillover) que el modelo MCO no estaba captando.

# Regresión espacial

Una conclusión que podemos ver es que tenemos sobredispersión, sin embargo no tenemos conteos iguales a 0 ya que manejamos los totales de casos por condado, por año.

## SLX

```{r include=FALSE}
# rezagamos las variables
X <- ohlung[,c('LMW68','LMW78','LFW68','LFW78')] %>% sf::st_drop_geometry()
WX_queen <- create_WX(X, queen_nb_w)
WX_knn8 <- create_WX(X,knn8_nb_w)
colnames(WX_queen) <- c('lag_LMW68','lag_LMW78','lag_LFW68','lag_LFW78')
colnames(WX_knn8) <- c('lag_LMW68','lag_LMW78','lag_LFW68','lag_LFW78')
data_ext_queen <- cbind(ohlung, WX_queen)
data_ext_knn8 <- cbind(ohlung, WX_knn8)


```

```{r modelos espaciales queen y knn8}
glmLSX_queen <- glm(casos_totales88~LMW68+LMW78+LFW68+LFW78+
                      lag_LMW68+lag_LMW78+lag_LFW68+lag_LFW78+
              offset(log(pop_total88)),
            data=data_ext_queen,family = poisson(link = "log"))
glmLSX_knn8 <- glm(casos_totales88~LMW68+LMW78+LFW68+LFW78+
                     lag_LMW68+lag_LMW78+lag_LFW68+lag_LFW78+
              offset(log(pop_total88)),
            data=data_ext_knn8,family = poisson(link = "log"))
anova(glmLSX_queen,glmLSX_knn8)
summary(glmLSX_queen)
summary(glmLSX_knn8)
```

```{r mroan para queen y knn8}
moran.test(residuals(glmLSX_queen), listw = queen_nb_w)
moran.test(residuals(glmLSX_knn8), listw = knn8_nb_w)

```
## Modelo Lag

```{r creacion rezagos en Y, include=FALSE}
# variable respuesta rezagada
Y <- ohlung[,c("casos_totales88","pop_total88")] %>% sf::st_drop_geometry()
WY_queen <- create_WX(Y,queen_nb_w) 
WY_knn8 <- create_WX(Y,knn8_nb_w)
colnames(WY_queen) <- c('lag_totales88','lag_pop88')
colnames(WY_knn8) <- c('lag_totales88','lag_pop88')
Y_ext_queen <- cbind(ohlung,WY_queen)
Y_ext_knn8 <- cbind(ohlung,WY_knn8)
```


```{r modelos lag}
glmlag_queen <- glm(casos_totales88~LMW68+LMW78+LFW68+LFW78+
                      lag_totales88+lag_pop88+
              offset(log(pop_total88)),
            data=Y_ext_queen,family = poisson(link = "log"))
glmlag_knn8 <- glm(casos_totales88~LMW68+LMW78+LFW68+LFW78+
                     lag_totales88+lag_pop88+
              offset(log(pop_total88)),
            data=Y_ext_knn8,family = poisson(link = "log"))
anova(glmlag_queen,glmlag_knn8)
summary(glmlag_queen)
summary(glmlag_knn8)
```
```{r mroan modelos lag}
moran.test(residuals(glmlag_queen), listw = queen_nb_w)
moran.test(residuals(glmlag_knn8), listw = knn8_nb_w)

```


